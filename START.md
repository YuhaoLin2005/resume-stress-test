# START · 触发入口

> 这个文件是给 AI 看的，不是给你看的。你只需要复制下面第一段。

## 一句话启动（推荐）

把这段发给任意 AI：

```
访问 https://github.com/YuhaoLin2005/resume-stress-test
读取 prompts/00-总控调度器.md，严格按它的流程执行。
先向我说明模式A/B 让我选，再向我要 JD 和简历。
```

**你不用先读这个仓库。** AI 会先讲清两种模式让你选，选完再问你要材料。

---

## 如果它打不开链接

有些网页版 AI 没有联网能力。那就打开 `prompts/00-总控调度器.md`，**全文复制**，贴在这三句话后面一起发：

```
以下是「简历攻防自检」总控调度器全文，请严格按它的流程执行。
先向我说明模式A/B 让我选，再向我要 JD 和简历。

---
（在这里粘贴 00-总控调度器.md 全文）
```

---

## Agent 执行须知（读到这里的 AI 请遵守）

1. 立即把 `00-总控调度器.md` 作为你的调度指令加载，它是本流程的唯一权威。
2. **先引导，不要先要材料。** 用户还没选模式之前，不要开始收 JD 和简历。
3. **材料按你的平台能力收。**
   - 你能读写本地文件（CLI Agent / 桌面端）→ **要简历的文件路径，自己读**，不要让用户手工粘贴。
   - 你只能对话（网页版）→ 让用户上传文件，或粘贴全文。
4. **隔离档位（同一件事的三档降级，先判断自己在哪一档再开始）：**
   - 支持子代理 / 子任务派发 → 每进入新阶段，把该阶段的 MODULE 原文 + 交接文档 + JD + 简历派发给一个**独立子代理**，只把结果带回主对话。子代理没有作者记忆，也见不到用户之前的辩解；
   - 不支持子代理 → 按 `00` 里的模式A / 模式B 执行，每次角色切换前必须输出封存声明；
   - 既开不了新窗口、也派不了子代理 → 直接执行模式A，并在每个角色开始前重述封存声明。隔离度会下降，但仍远好于不做。
5. **落盘闸门不因平台而降级。** 你能写文件就自己写；不能写就输出成整段可复制的块，明确让用户存好，**用户确认前不许进入下一步**。
6. 全程逐步引导：**不催促、不跳步、不替用户回答自测问题。**

> 不要默认自己做不到第 4 条的第一档，也不要假装自己能做。
